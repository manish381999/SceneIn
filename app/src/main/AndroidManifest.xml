<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- All your existing permissions are correct -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- You might not need CAMERA permission here if you only use the system's camera app -->
    <!-- but it's good practice to add it if you interact with the camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".SceneInApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">
        <activity
            android:name=".settings.persentation.screen.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn"/>
        <activity
            android:name=".profile.persentation.screen.ProfilePicturePreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".profile.persentation.screen.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".credentials.presentation.screens.CropActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".notifications.presentation.screens.ConnectionRequestsActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".notifications.presentation.screens.NotificationsActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".tickets.persentation.screens.ManageListingActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".tickets.persentation.screens.PurchasedTicketDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".tickets.persentation.screens.PayoutSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".tickets.persentation.screens.CheckoutActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:exported="false"
            android:theme="@style/Theme.Razorpay" />
        <activity
            android:name=".tickets.persentation.screens.TicketDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".tickets.persentation.screens.SellTicketActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".chat.persentation.screens.MediaViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".chat.persentation.screens.ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".profile.persentation.screen.UserProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".discover.presentation.screens.EventDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".credentials.presentation.screens.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".BaseActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".credentials.presentation.screens.OtpVerificationActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".credentials.presentation.screens.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.VibeIn" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification_icon" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>


    </application>

</manifest>