<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.VibeIn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/textOnPrimary</item>
        <item name="android:statusBarColor">@android:color/black</item>



        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/textOnSecondary</item>

        <!-- Background and Surface -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/textPrimary</item>
        <item name="colorSurfaceVariant">@color/colorSurfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/colorOnSurfaceVariant</item>


        <!-- Error colors -->
        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/textOnPrimary</item>

        <!-- General text -->
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/colorBackground</item>



    </style>

    <style name="TabTextStyle" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

    <style name="ThemeOverlay.App.BottomSheetDialog" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <!-- This attribute points to the style that will be applied to the bottom sheet's container -->
        <item name="bottomSheetStyle">@style/Widget.App.BottomSheet.Modal</item>
    </style>

    <!-- This is the style for the bottom sheet's container -->
    <style name="Widget.App.BottomSheet.Modal" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <!-- Here we apply our custom drawable with the rounded corners -->
        <item name="android:background">@drawable/bottom_sheet_background</item>
    </style>

    <!-- Style for the container that holds the filter chips -->
    <style name="Widget.App.ChipGroup" parent="Widget.MaterialComponents.ChipGroup">
        <item name="chipSpacingHorizontal">8dp</item>
        <item name="singleLine">true</item>
    </style>


    <!-- Your existing style for an individual filter chip -->
    <style name="Widget.App.Chip" parent="Widget.MaterialComponents.Chip.Filter">
        <!-- Use our new color selectors to make the chip theme-aware -->
        <item name="android:textColor">@color/chip_text_color</item>
        <item name="chipBackgroundColor">@color/chip_background_color</item>

        <!-- No stroke for a cleaner, more modern look -->
        <item name="chipStrokeWidth">0dp</item>
        <item name="android:elevation" tools:ignore="NewApi">2dp</item>

        <!-- ============================================= -->
        <!-- == THE FIX: THIS LINE HIDES THE CHECKMARK === -->
        <!-- ============================================= -->
        <item name="checkedIconVisible">false</item>

    </style>

    <style name="Theme.Razorpay" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- This makes the status bar translucent, creating a seamless feel when Razorpay launches. -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- This prevents a strange flicker or "blink" when the activity opens. -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorBackground</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>

        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.VibeIn</item>
    </style>

    <style name="ToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:fontFamily">@font/poppins_bold</item>
        <item name="android:textSize">@dimen/_16ssp</item>
    </style>





</resources>
